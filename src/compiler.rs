use crate::parser::Expr;


// out -> contains the bytecode for the vm
pub fn compile_expression (expr : &Expr, out : &mut Vec<i32>) {
    // match expr {
    //     Expr::Number(n) => {
    //     }
    // }
}