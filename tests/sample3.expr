define (n 7
    define (a 1
        define (b 1
            define (count 2
                while (count < n
                    define (temp (a + b)
                        define (a b
                            define (b temp
                                define (count (count + 1)
                                    b
                                )
                            )
                        )
                    )
                )
            )
        )
    )
)